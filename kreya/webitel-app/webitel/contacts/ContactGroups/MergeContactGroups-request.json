{
  "xJsonMask": [
    "Savings Account one-to-one Metrics",
    "Crossing"
  ],
  "fields": [
    "capacitor feed GB",
    "SMTP"
  ],
  "contactId": "{{ faker.random.uuid }}",
  "input": [
    {
      "etag": "content models Solutions",
      "group": {
        "id": "{{ faker.random.uuid }}",
        "type": "{{ faker.database.type }}",
        "name": "{{ faker.name.last_name }}"
      }
    },
    {
      "etag": "Granite",
      "group": {
        "id": "{{ faker.random.uuid }}",
        "type": "{{ faker.database.type }}",
        "name": "{{ faker.name.last_name }}"
      }
    }
  ]
}