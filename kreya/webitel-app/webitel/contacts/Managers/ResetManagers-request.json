{
  "fields": [
    "adapter Money Market Account",
    "Group deposit initiatives"
  ],
  "contactId": "{{ faker.random.uuid }}",
  "input": [
    {
      "etag": "Direct Danish Krone",
      "primary": true,
      "user": {
        "id": "{{ faker.random.uuid }}",
        "type": "{{ faker.database.type }}",
        "name": "{{ faker.name.last_name }}"
      }
    },
    {
      "etag": "redundant",
      "primary": false,
      "user": {
        "id": "{{ faker.random.uuid }}",
        "type": "{{ faker.database.type }}",
        "name": "{{ faker.name.last_name }}"
      }
    }
  ]
}