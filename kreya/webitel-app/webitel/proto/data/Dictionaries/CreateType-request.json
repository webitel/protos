{
  "repo": "{{ faker.random.words(1) }}",
  "input": {
    "name": "{{ faker.random.words(1) }}",
    "about": "{{ faker.lorem.paragraph }}",
    "fields": [
      {
        "id": "first",
        "name": "{{ faker.name.last_name }}",
        "kind": "int32",
        "default": "reboot",
        "readonly": true,
        "required": true,
        "disabled": false,
      },
      {
        "id": "second",
        "name": "{{ faker.name.last_name }}",
        "kind": "string",
        "default": "Cambridgeshire array",
        "readonly": false,
        "required": true,
        "disabled": true,
      },
    ],
    "primary": "first",
    "display": "second",
  }
}