syntax = "proto3";

package webitel.messaging.thread.v1;
import "group_thread.proto";

option go_package = "github.com/webitel/protos/messaging/thread/v1;impb";

// This service provides methods for working with group threads.
service GroupThread {
  // Creates a new group thread.
  rpc CreateGroupThread(CreateGroupThreadRequest) returns (CreateGroupThreadResponse) {
    option (google.api.http) = {
      post: "messaging/v1/thread"
      body: "*"
    };
  }

  // Searches for a group thread based on the provided request.
  rpc SearchThread(SearchThreadRequest) returns (SearchThreadResponse) {
    option (google.api.http) = {
      get: "messaging/v1/thread"
    };
  }

  // Add a member to a group thread.
  rpc AddMemberToGroupThread(AddMemberToGroupThreadRequest) returns (AddMemberToGroupThreadResponse) {
    option (google.api.http) = {
      post: "messaging/v1/thread/{thread_id}/members"
      body: "*"
    };
  }
}
