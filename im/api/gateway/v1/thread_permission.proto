syntax = "proto3";

package webitel.im.api.gateway.v1;

import "google/api/annotations.proto";
import "buf/validate/validate.proto";

// ThreadPermissionService defines the gRPC service for managing permissions for thread members.
service ThreadPermission {
    // Get returns all permissions for the specified thread. Optional filter by member_id to get permissions for a specific member.
  rpc Get(GetThreadPermissionsRequest) returns (GetThreadPermissionsResponse);
  // Update updates permissions for a specific member in the thread. The request must include the thread_id and member_id, along with the permissions to update.
  rpc Update(UpdateThreadPermissionsRequest) returns (UpdateThreadPermissionsResponse);
}

message GetThreadPermissionsRequest {
  string member_id = 1[
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).required = true
  ];
}

message GetThreadPermissionsResponse {
  ThreadPermissions permissions = 1;

}

message ThreadPermissions {
  string id         = 1;
  string member_id  = 2;
  int64  created_at = 4;
  int64  updated_at = 5;

  bool can_send_messages              = 6;
  bool can_add_members                = 7;
  bool can_remove_members             = 8;
  bool can_change_members_permissions = 9;
  bool can_change_thread_info         = 10;
}

message UpdateThreadPermissionsRequest {
  string member_id = 1[
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).required = true
  ];

  optional bool can_send_messages              = 2;
  optional bool can_add_members                = 3;
  optional bool can_remove_members             = 4;
  optional bool can_change_members_permissions = 5;
  optional bool can_change_thread_info         = 6;
}

message UpdateThreadPermissionsResponse {
  ThreadPermissions permissions = 1;
}