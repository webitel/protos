version: v2
# name: buf.build/webitel/messaging

# Dependencies shared by all modules in the workspace. Must be modules hosted in the Buf Schema Registry.
# The resolution of these dependencies is stored in the buf.lock file.
deps:
  # The latest accepted commit.
  - buf.build/googleapis/googleapis

modules:
  - path: .
    excludes:
    # - contact/v1
    - thread/v1
    - message/v1
  #   - proto/include
  # - path: proto/include
  # - path: proto/webitel/messaging/v1
    # includes:
    # - proto

breaking:
  use:
    - FILE
