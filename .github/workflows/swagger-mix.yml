on: [push]

jobs:
  swagger-mix:
    runs-on: ubuntu-latest
    name: Merge a swagger files 
    steps:
      - name: Mix
        id: mix
        uses: Dtsnko/swagger-mix-action@main
        with:
          directory: 'swagger'
